<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Selected state uses your app's favorite color -->
    <item android:color="@color/nazmi_fav_dark" android:state_checked="true"/>
    <!-- Unselected state uses a very subtle gray that matches your app's theme -->
    <item android:color="#AAAAAA"/>
</selector>